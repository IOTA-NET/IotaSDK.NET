namespace IotaSDK.NET.Domain.Features
{
    /**
     * All of the feature block types.
     */
    public enum FeatureType
    {
        /** A Sender feature. */
        Sender = 0,
        /** An Issuer feature. */
        Issuer = 1,
        /** A Metadata feature. */
        Metadata = 2,
        /** A Tag feature. */
        Tag = 3,
    }

}
